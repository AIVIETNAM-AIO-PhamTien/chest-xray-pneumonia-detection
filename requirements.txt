# Runtime
# Pinned to the combination verified in .venv (matches
# artifacts/notebook_complete/environment.json's local smoke-test run) --
# this project depends heavily on exact reproducibility, so unbounded ">="
# ranges are a real drift risk. Bump deliberately, then re-run
# `python -m src.train --config configs/protocol_b.yaml ...` and pytest.
torch==2.13.0
torchvision==0.28.0
numpy==2.4.6
pandas==3.0.5
scikit-learn==1.9.0
matplotlib==3.11.1
pillow==12.3.0
pyyaml==6.0.3
tqdm==4.69.1
wandb==0.28.1

# Dev / QA
black==26.5.1
isort==8.0.1
flake8==7.3.0
pytest==9.1.1

